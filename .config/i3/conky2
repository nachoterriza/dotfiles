

background no
out_to_x no
out_to_console yes
update_interval 2.0
total_run_times 0
use_spacer none

TEXT

 ${if_mpd_playing} ${mpd_smart} ${endif}   \
 ${exec cat /sys/devices/platform/coretemp.0/hwmon/hwmon1/temp1_input | head -c 2}ºC / ${exec nvidia-smi --query-gpu=temperature.gpu --format=csv,noheader}ºC   \
 ${downspeedf eno1}|${upspeedf eno1}   \
 ${exec amixer get Master | grep -oE "[[:digit:]]*%" | sed -n 1p}   \
 ${battery_percent}%   \
 ${time %a %d %b}   \
 ${time %H:%M} 
